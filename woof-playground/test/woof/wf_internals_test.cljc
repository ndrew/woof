(ns woof.wf-internals-test
  (:require
    [cljs.test :refer-macros [use-fixtures deftest is testing async]]
    ;[clojure.core.async :as async :refer [go go-loop]]
    ;[clojure.test :refer :all]

    ;[woof.data :as d]
    ;[woof.wf :as wf]
    ;[woof.wfc :as wfc]

    ;[woof.utils :as u]

))


(deftest hello
  (is (= true false))
  )

;; test here internal protocols from woof core

;; state

;; steps

;;
