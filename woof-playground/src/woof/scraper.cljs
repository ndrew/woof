(ns woof.scraper
  (:require
    [woof.playground :as playground]
    [woof.browser :as browser]
    ))

;; reference browser and playground so both scraping workflows and playground will be compiled
